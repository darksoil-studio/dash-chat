use dashchat_node::{topic::kind, Node, Topic};
use tauri::State;

// #[tauri::command]
// pub fn my_device_group_topic(node: State<'_, Node>) -> Topic<kind::Chat> {
//     node.device_group_topic()
// }
