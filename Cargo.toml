[profile.release]
opt-level = 3
panic = "abort"
lto = "fat"
codegen-units = 1
# inherits = "release"

[workspace]
members = [
  # "crates/dashchat-node", 
  "src-tauri",
]
resolver = "2"

[workspace.dependencies]
