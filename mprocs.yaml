procs:
  agent1:
    shell: pnpm tauri dev --no-watch
  agent2:
    shell: pnpm tauri dev --no-watch
  # push-notifications-service:
  #   shell: test-push-notifications-service ./service-account-key.json somesecretnetworkseedv0.5.0
  # safehold-service:
  #   shell: test-safehold-service somesecretnetworkseedv0.5.0
  ui:
    shell: pnpm -F ./ui start
  android:
    shell: adb uninstall studio.darksoil.dashchat | true && pnpm tauri android dev
  logcat:
    shell: adb logcat | grep -F "`adb shell ps | grep studio.darksoil.dashchat | tr -s [:space:] ' ' | cut -d' ' -f2`"
